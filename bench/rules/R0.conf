# Ruleset R0 -- engine ON, zero rules.
#
# Isolates the per-request cost of the connector itself: building the
# transaction, feeding the request line / headers, running the (empty) phase
# machine, tearing down. No rule matching, no audit, no blocking.
#
# Intentionally empty.
